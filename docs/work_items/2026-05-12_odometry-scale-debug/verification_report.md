# 验证报告

## 验证摘要

- 日期：2026-05-12
- 功能点：里程计比例修正
- 负责人：Codex

## 已达到的验证层级

- Level 0 静态审查：是
- Level 1 桌面验证：是
- Level 2 操作人测试准备：是
- Level 3 硬件验证：部分完成

## 桌面验证证据

- 已执行命令：`cmake --build cmake-build-debug`
- 结果：成功
- 最近一次构建说明：在将 `L / R` 手动转向命令恢复为相对转向语义后构建通过；RAM `98.85%`，FLASH `22.06%`
- 覆盖范围限制：桌面环境无法证明实体车轮比例精度

## 硬件验证证据

- 测试计划：`hardware_test_plan.md`
- 手工测试记录：`manual_test_record.md`
- 结果：已完成首轮人工量距，但仍需复测
- 已补充给操作人的辅助内容：结果判读矩阵，用于区分比例偏差、左右不对称、进度统计错误和复位残留
- 已补充给操作人的辅助内容：可直接复制回传的回复模板，覆盖基线、转向、前进、倒车以及 `D...` 输出
- 基线结果：`R0` 后 `ODOM / MOVE / EST / CTRL` 均回到零附近，说明复位链路基本干净
- 手动转向结果：`L / R` 已恢复为相对转向语义，但转向过程仍有卡顿和可能的超调
- 前进 1.0 m 结果：`ODOM x=0.984 m`、`MOVE progress=0.957 m`，实体仅约 `0.82 m`，`ENC dl/dr = 0.793 / 1.176 m`
- 倒车 1.0 m 结果：记录里的 `ENC dl/dr = -0.337 / 0.430 m` 是累计量，不是独立倒车段。若与前进阶段累计值做差，倒车单段更接近 `dl/dr = -1.130 / -0.746 m`，说明问题更像反向左右轮不对称和比例偏差，而不能直接下结论说“符号链路错误”
- 过程限制：本轮记录在前进和倒车前没有重新 `R0`，且 `D0.82` 建议基于累计轮程，混入了前序摆位和前一段运动，不能直接当作单段标定真值
- 已补充固件改进：`D...` 现优先使用最近一次完成的 `P{x},{y}` 相对位移段轮程，避免累计残留污染标定建议；`O` 也新增 `LMOVE ...` 直接显示最近一次单段轮程；`D...` 还新增 `CAL diag ...` 直接打印左右轮误差百分比和不对称程度

## 仍然存在的缺口

- 需要在刷入新版固件后重新做一次“单段前进 -> `O` -> `D...`”隔离标定
- 需要确认前进方向的主要误差是否稳定地表现为右轮里程偏大、左轮略偏小
- 需要确认倒车方向的单段 `LMOVE dl/dr` 是否稳定表现为左右不对称，以及反向系数是否需要分开回写
- 需要确认新版 `D...` 输出的 `CAL source=last_move` 是否与当前这一段测试严格对应
- 需要确认 `P1.0,0.0` 的 `MOVE progress` 已不再是主误差源，当前主因是否已经明确收敛到轮比例/左右不对称
- 需要确认转向环的卡顿和可能超调是否会影响后续摆位与标定重复性

## 发布 / 合并建议

- 可以继续推进，但下一轮必须基于新版固件完成隔离复测
